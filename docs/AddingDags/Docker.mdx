---
id: adding-dags-docker
title: Adding DAGs Docker
sidebar_label: Docker
slug: /adding-dags/docker
description: Set up a CI/CD pipeline to test and deploy Apache Airflow DAGs to a self-hosted Docker Compose environment.
keywords:
  - apache airflow
  - docker
  - dag
  - cicd
  - github actions
  - deployment
  - docker compose
tags: [Apache Airflow, Docker, CI/CD, DAGs]
---

# Adding DAGs — Docker

This guide extends the [Docker Installation](/docs/Installation/Docker) setup. After Airflow is running in Docker, the next step is getting DAG code from Git into the `./dags` folder **safely and automatically** — with tests running before anything reaches production.
